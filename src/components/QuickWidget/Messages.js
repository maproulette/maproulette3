import { defineMessages } from "react-intl";

/**
 * Internationalized messages for use with QuickWidget
 */
export default defineMessages({
  widgetFailure: {
    id: "QuickWidget.failedToLoad",
    defaultMessage: "Widget Failed",
  },
});
