import { defineMessages } from 'react-intl'

/**
 * Internationalized messages for use with InspectTaskControls
 */
export default defineMessages({
  deleteWidget: {
    id: "Widgets.DeleteWidget.label",
    defaultMessage: "Delete Widget",
  },
})
