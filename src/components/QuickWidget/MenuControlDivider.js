import React, { Component } from 'react'

/**
 * Renders a divider between MenuControls in a grid block
 */
export default class MenuControlDivider extends Component {
  render() {
    return <hr className="dropdown-divider" />
  }
}
