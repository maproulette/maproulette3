import { defineMessages } from 'react-intl'

/**
 * Internationalized messages for use with TaskCommentsModal
 */
export default defineMessages({
  header: {
    id: 'TaskCommentsModal.header',
    defaultMessage: "Comments",
  },
})
