import { defineMessages } from 'react-intl'

/**
 * Internationalized messages for use with Activity
 */
export default defineMessages({
  loadMoreLabel: {
    id: "Following.Activity.controls.loadMore.label",
    defaultMessage: "Load More",
  },
})
