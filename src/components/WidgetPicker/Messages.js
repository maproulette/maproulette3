import { defineMessages } from 'react-intl'

/**
 * Internationalized messages for use with WidgetPicker
 */
export default defineMessages({
  pickerLabel: {
    id: "WidgetPicker.menuLabel",
    defaultMessage: "Add Widget",
  },
})
