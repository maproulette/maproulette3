import { defineMessages } from 'react-intl'

/**
 * Internationalized messages for use with AdminPane.
 */
export default defineMessages({
  control: {
    id: 'SignIn.control.label',
    defaultMessage: "Sign in to Continue",
  },
})
