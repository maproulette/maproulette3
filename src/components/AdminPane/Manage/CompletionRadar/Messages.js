import { defineMessages } from 'react-intl'

/**
 * Internationalized messages for use with CompletionRadar.
 */
export default defineMessages({
  heading: {
    id: "CompletionRadar.heading",
    defaultMessage: "Tasks Completed: {taskCount, number}",
  },
})
