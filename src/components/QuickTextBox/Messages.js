import { defineMessages } from 'react-intl'

/**
 * Internationalized messages for use with QuickTextBox
 */
export default defineMessages({
  saveLabel: {
    id: "QuickTextBox.controls.save.label",
    defaultMessage: "Save",
  },
})
