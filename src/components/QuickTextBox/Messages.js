import { defineMessages } from "react-intl";

/**
 * Internationalized messages for use with QuickTextBox
 */
export default defineMessages({
  saveLabel: {
    id: "Admin.EditProject.controls.save.label",
    defaultMessage: "Save",
  },
});
