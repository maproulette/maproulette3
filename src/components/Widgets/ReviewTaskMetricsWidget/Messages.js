import { defineMessages } from 'react-intl'

/**
 * Internationalized messages for use with TaskCompletionWidget
 */
export default defineMessages({
  label: {
    id: "Widgets.ReviewTaskMetricsWidget.label",
    defaultMessage: "Review Task Metrics",
  },
  title: {
    id: "Widgets.ReviewTaskMetricsWidget.title",
    defaultMessage: "Task Status",
  }
})
