import { defineMessages } from 'react-intl'

/**
 * Internationalized messages for use with TagMetrics
 */
export default defineMessages({
  noTags: {
    id: "TagMetrics.noTags.label",
    defaultMessage: "No tags to display.",
  }
})
