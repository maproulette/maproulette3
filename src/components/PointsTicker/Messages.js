import { defineMessages } from 'react-intl'

/**
 * Internationalized messages for use with PointsTicker
 */
export default defineMessages({
  label: {
    id: "PointsTicker.label",
    defaultMessage: "My Points",
  },
})
