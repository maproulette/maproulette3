import { defineMessages } from 'react-intl'

/**
 * Internationalized messages for use with HelpPopout.
 */
export default defineMessages({
  help: {
    id: "HelpPopout.control.label",
    defaultMessage: "Help"
  },
})
