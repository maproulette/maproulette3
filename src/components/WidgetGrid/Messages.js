import { defineMessages } from "react-intl";

/**
 * Internationalized messages for use with WidgetGrid
 */
export default defineMessages({
  expandPanel: {
    id: "WidgetGrid.controls.expandPanel.label",
    defaultMessage: "Expand panel",
  },
  collapsePanel: {
    id: "WidgetGrid.controls.collapsePanel.label",
    defaultMessage: "Collapse panel",
  },
});
