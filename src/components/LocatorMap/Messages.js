import { defineMessages } from 'react-intl'

/**
 * Internationalized messages for use with LocatorMap
 */
export default defineMessages({
  startChallengeLabel: {
    id: "LocatorMap.controls.startChallenge.label",
    defaultMessage: "Start Challenge"
  },
})
