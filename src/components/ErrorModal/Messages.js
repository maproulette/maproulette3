import { defineMessages } from 'react-intl'

/**
 * Internationalized messages for use with ErrorModal
 */
export default defineMessages({
  title: {
    id: "ErrorModal.title",
    defaultMessage: "Oops!",
  },
})
