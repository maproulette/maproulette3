import { defineMessages } from 'react-intl'

/**
 * Internationalized messages for use with MoreOptionsControls
 */
export default defineMessages({
  moreOptionsLabel: {
    id: "Task.controls.moreOptions.label",
    defaultMessage: "More Options"
  },
})

