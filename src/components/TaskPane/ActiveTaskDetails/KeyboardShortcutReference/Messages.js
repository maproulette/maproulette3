import { defineMessages } from 'react-intl'

/**
 * Internationalized messages for use with KeyboardShortcutReference.
 */
export default defineMessages({
  control: {
    id: "KeyboardShortcuts.control.label",
    defaultMessage: "Keyboard Shortcuts",
  },

  keyboardShortcuts: {
    id: 'ActiveTask.keyboardShortcuts.label',
    defaultMessage: 'View Keyboard Shortcuts',
  }
})
