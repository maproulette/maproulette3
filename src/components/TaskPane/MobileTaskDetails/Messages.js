import { defineMessages } from "react-intl";

/**
 * Internationalized messages for use with MobileTaskDetails.
 */
export default defineMessages({
  instructions: {
    id: "Admin.EditTask.form.instruction.label",
    defaultMessage: "Instructions",
  },
});
