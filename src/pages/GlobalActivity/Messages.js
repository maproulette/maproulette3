import { defineMessages } from "react-intl";

/**
 * Internationalized messages for use with GlobalActivity
 */
export default defineMessages({
  title: {
    id: "GlobalActivity.title",
    defaultMessage: "Global Activity",
  },
});
