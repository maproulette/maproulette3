# StatusBadge

**Source**: `src/components/shared/StatusBadge.tsx`

A pill-shaped badge showing whether an entity is discoverable (enabled) or not. Shows an eye icon with green background when enabled, eye-off icon with gray background when disabled. Used on project and challenge cards across management pages.
