# EntityGrid

**Source**: `src/components/shared/EntityGrid.tsx`

A generic typed grid container that renders a collection of items using a custom render function. When the list is empty, it shows a configurable empty state with icon, title, description, and optional action button. Used for project and challenge grids on management pages.
