# SearchBar

**Source**: `src/components/shared/SearchBar.tsx`

A text input with a search icon on the left. Takes a value, onChange callback, and optional placeholder. Used across all management and admin pages for filtering lists.
